to use vault mcp

    "vault": {
      "command": "path to the venv \\.venv\\Scripts\\python.exe",
      "args": [
        "path to script \\vault-mcp\\vault-mcp.py",
        "path to the vault root folder"
      ]
    },
